# Server only. Never put a real private key in the repository or browser.
FIREBASE_PROJECT_ID=
FIREBASE_DATABASE_URL=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_WEB_API_KEY=
# Comma-separated Firebase Authentication UIDs, NOT emails or usernames.
ADMIN_UIDS=
# Exact production origin, no trailing slash. Local: http://localhost:5173
APP_ORIGIN=http://localhost:5173
