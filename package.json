{
  "name": "amenza-private-backend",
  "private": true,
  "scripts": {
    "build": "node scripts/build.cjs",
    "start": "node --env-file-if-exists=.env server.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "firebase-admin": "14.4.0"
  },
  "overrides": {
    "gaxios": {
      "uuid": "11.1.1"
    }
  }
}
